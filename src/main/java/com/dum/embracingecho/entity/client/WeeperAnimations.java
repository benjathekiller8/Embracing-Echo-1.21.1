package com.dum.embracingecho.entity.client;

import net.minecraft.client.animation.AnimationDefinition;

public class WeeperAnimations {
    public static final AnimationDefinition ANIM_WEEPER_IDLE = AnimationDefinition.Builder.withLength(0.0F).looping()

            .build();

    public static final AnimationDefinition ANIM_WEEPER_WALK = AnimationDefinition.Builder.withLength(0.0F).looping()

            .build();
}
